---
- name: Create canonical directories
  ansible.builtin.file:
    path: "/srv/unix-time-machine/{{ item }}"
    state: directory
    owner: root
    group: root
    mode: "0755"
  loop:
    - media
    - golden
    - state
    - sessions
    - snapshots
    - logs
    - reports
